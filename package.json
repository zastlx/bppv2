{
  "name": "bppweb",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "publish": "tsc && vite build && gh-pages -d dist -b gh-pages-dist",
    "preview": "tsc && vite build && vite preview"
  },
  "dependencies": {
    "preact": "^10.19.6"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.8.2",
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  }
}
